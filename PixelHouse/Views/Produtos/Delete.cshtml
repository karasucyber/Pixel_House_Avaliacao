@model PixelHouse.Models.Produto

<h1>Excluir Produto</h1>

<h3>Tem certeza de que deseja excluir este produto?</h3>
<h6>Essa ação pode causar erros em produtos que já estão em processo de compra ou venda.</h6>
<div>
    <h4>@Model.Nome</h4>
    <dl class="row">
        <dt class="col-sm-2">
            ID
        </dt>
        <dd class="col-sm-10">
            @Model.Id
        </dd>
           <dt class="col-sm-2">
            Nome
        </dt>
        <dd class="col-sm-10">
            @Model.Nome
        </dd>
           <dt class="col-sm-2">
            Estoque Atual
        </dt>
        <dd class="col-sm-10">
            @Model.QuantidadeEmEstoque
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Excluir" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
